# Backend environment example for todo-backend

# Preferred origin allowed by CORS (React dev server by default)
REACT_APP_FRONTEND_URL=http://localhost:3000

# Optional: backend URL (not required by server, used as fallback for CORS origin logic)
REACT_APP_BACKEND_URL=http://localhost:4000

# Environment indicators and logging
REACT_APP_NODE_ENV=development
REACT_APP_TRUST_PROXY=false
REACT_APP_LOG_LEVEL=dev

# Health endpoint customization (default is /healthz)
REACT_APP_HEALTHCHECK_PATH=/healthz

# Server port (defaults to 4000 if not provided)
PORT=4000
